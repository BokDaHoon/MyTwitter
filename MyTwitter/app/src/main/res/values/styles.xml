<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/twitterDefaultColor</item>
        <item name="colorPrimaryDark">@color/twitterDefaultColor</item>
        <item name="colorAccent">@color/twitterDefaultFocusColor</item>
    </style>

    <style name="AppThemeNoBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/twitterDefaultColor</item>
        <item name="colorPrimaryDark">@color/twitterDefaultColor</item>
        <item name="colorAccent">@color/twitterDefaultFocusColor</item>
        <!-- No Title Bar-->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>


</resources>
